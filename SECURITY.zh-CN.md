# 安全说明

## 本地控制边界

Android Link 尽量把控制面限制在当前 Mac 用户和本机：

- 浏览器控制服务只监听 `127.0.0.1`。
- AI IPC 使用当前用户 Android Link 数据目录下的 Unix Domain Socket。
- 运行时 Secret 和 Socket 文件使用严格的本地权限。
- 浏览器请求需要随机生成的控制 Token。
- 服务会校验本地 Host / Origin，并限制请求体大小、设置 CSP。

## 敏感数据

设备序列号、截图、UI Tree、输入文字、登录页面和导出日志都可能包含敏感信息。

诊断导出设计上不会主动包含截图、UI Tree、输入文字、控制页 Token 和原始 ADB 序列号，但在公开上传前仍建议人工检查一次。

## 安全问题反馈

可以提交 GitHub Issue，但不要在公开 Issue 中直接粘贴密码、Token、真实设备序列号或包含个人信息的截图。如果问题复现必须依赖敏感信息，请先只描述问题类型。
